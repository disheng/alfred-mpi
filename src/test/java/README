Add jars that doesn't have a maven repo here with

groupId.artifactId[.source]_version[.SNAPSHOT].jar

and then run py script in root project directory

(Reference: https://github.com/nikita-volkov/install-to-project-repo)

p.s.: jar will be pushed to git only in repo directory